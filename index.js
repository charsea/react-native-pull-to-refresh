/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import PullToRefresh from './App';

AppRegistry.registerComponent(appName, () => PullToRefresh);
